# 环境变量示例（复制为 .env 后按需填写）

# 数据库连接（Zeabur MySQL/PostgreSQL）
# 例：mysql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=""

# JWT 配置
JWT_SECRET="please-change-in-production-min-32-chars"
JWT_EXPIRES_IN="604800"

# 端口配置（单服务部署：前端 3000、后端 3001）
PORT=3000
SERVER_PORT=3001

# CORS 与代理配置
# 前端访问域名（本地/生产），用于后端 CORS
CLIENT_URL="http://localhost:3000"
# Next.js 代理到后端地址（单服务容器内回环地址）
SERVER_URL="http://127.0.0.1:3001"

# 上传目录与限制
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=10485760

# Node 环境
NODE_ENV=production

# 管理员初始化（首次部署可用，可在 Zeabur 命令中临时覆盖）
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="changeMe123456"