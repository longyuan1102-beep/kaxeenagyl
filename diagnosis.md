# 🔍 诊断问题

## 登录失败原因检查

1. **后端未启动** → 前端无法连接
2. **数据库未初始化** → 没有用户数据  
3. **Prisma客户端未生成** → 连接失败

---

## ✅ 正确启动顺序

### 第1步：检查 .env
```bash
cat .env
```
必须有 DATABASE_URL

### 第2步：生成Prisma客户端
```bash
npm run db:generate
```

### 第3步：初始化数据库
```bash
npm run db:push
```

### 第4步：创建管理员
```bash
npm run create-admin
```

### 第5步：启动后端
```bash
cd server
npx nest start --watch
```

### 第6步：启动前端
```bash
npm run dev
```

---

## ⚠️ 当前问题

根据你的截图，很可能是：
1. 后端未启动
2. 或数据库没有用户

请检查：
- 后端的终端窗口是否在运行？
- 是否看到 "服务器运行在 3001"？
- 是否运行过 `npm run create-admin`？

---

**告诉我检查结果，我会立即解决！**
